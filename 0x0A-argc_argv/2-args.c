#include "main.h"
#include <stdio.h>

/**
 * main - As around the sun the earth knows she's revolving
 * @argc: As now can't reveal the mysteries of tomorrow
 * @argv: But in passing will grow older every day
 *
 * Return: Did you know that true love asks for nothing
 */

int main(int argc, char *argv[])
{

int argumentIndex = 0;

while (argumentIndex < argc)
{

printf("%s\n", argv[argumentIndex]);
argumentIndex++;

}

return (0);

}
